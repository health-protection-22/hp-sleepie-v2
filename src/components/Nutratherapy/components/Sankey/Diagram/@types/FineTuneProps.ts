import { DietarySupplementProps } from '../../../../@types/DietarySupplementProps';

export type FineTuneProps = {
  subHealthGoal: string;
  option: string;
  dietarySupplements: DietarySupplementProps[];
};
